numpy
pandas
google-genai
fastapi
uvicorn[standard]
# Optional: MCP server runtime (install if you want to run as an MCP server)
mcp[cli]
fastmcp
# Optional: crew AI runtime (if you want to use an installed Crew AI agent)
litellm
crewai
crewai-tools[mcp]